
import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';
import AllDivisionsDashboard from './components/AllDivisionsDashboard';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <AllDivisionsDashboard />
  </React.StrictMode>
);
